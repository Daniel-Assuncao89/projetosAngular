export interface Estados {
    sigla: string
    nome: string
}

export interface Cidade {
    nome: string
}

export interface Ponto {
    nome: string
    estado: string
    cidade: string
    ponto: string
    inicio: string
    termino: string
}
